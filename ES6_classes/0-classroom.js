export default class ClassRoom {
  // define a class named ClassRoom & export it as the default export
  constructor(maxStudentsSize) {
    // Define a constructor function that takes a parameter named maxStudentsSize
    this._maxStudentsSize = maxStudentsSize;
    // Assign the value of the maxStudentsSize parameter to the instance variable _maxStudentsSize
  }
}
