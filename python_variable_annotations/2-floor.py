#!/usr/bin/env python3
"""Module to provide a function to compute the floor of a float."""

import math


def floor(n: float) -> int:
    """Return the floor of a float.

    Args:
        n (float): The floating point number.

    Returns:
        int: The floor of the float.
    """
    return math.floor(n)
